import random



def random_walk(substitution_list, real_label):
    return 



def random_one_round(substitution_list, real_label):

    size = len(substitution_list)
    random_int_list = random.sample(range(0, size), size // 2)
    for index in random_int_list:
        substitution = substitution_list[index]
        substitution.candidate_token
        